server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cognitron?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Dp810@310
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
  queue: content-crawl-queue
  exchange: content-exchange
  routing-key: crawl.key

cognitron:
  crawl:
    global-max-pages-per-24h: 200
