server:
  port: 8081


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/?user=root/cognitron?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Dp810@310
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

rabbitmq:
  host: localhost
  port: 15672
  username: guest
  password: guest
  queue: cognitron-crawl
  exchange: cognitron-exchange
  routing-key: crawl.key
